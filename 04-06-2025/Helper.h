#ifndef HELPER_CLASS_H
#define HELPER_CLASS_H

#include <string>

class Helper {
	public:
		int factorial(int n);
		int addNumber(int n, std::string method);
		int addVector(int x[], int n);
};

#endif